# Build
bin/
obj/
*.user
.vs/
.vscode/
.claude/

# Secretos — NUNCA subir a git
.env.emergency
.env
appsettings.Development.json
appsettings.Production.json
*.pfx
*.key


# Google OAuth2 — credenciales y token
google_credentials.json
google_token.json

# Google OAuth2 — respuestas de token generadas
Google.Apis.Auth.OAuth2.Responses.TokenResponse*
TokenResponse*
*.sql
.geminirules
.clauderules

# Migraciones de EF Core — el esquema real se gestiona con scripts SQL manuales,
# no queremos exponer la estructura de tablas/columnas via el historial de migraciones
Migrations/